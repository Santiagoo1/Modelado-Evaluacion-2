<EditForm Model="cliente" OnValidSubmit="onValidSubmit">
     <div class="form-group">
        <label>Nombre</label>
        <div>
            <InputText class="form-control" @bind-Value="@cliente.Nombre" />
            <ValidationMessage For="@(() => cliente.Nombre)" />
        </div>
     </div>


    <div class="form-group">
        <label>IdCliente</label>
        <div>
            <InputNumber class="form-control" @bind-Value="@cliente.Idcliente" />
            <ValidationMessage For="@(() => cliente.Idcliente)" />
        </div>
    </div>


    <div class="form-group">
        <label>Cuanto Deben</label>
        <div>
            <InputNumber class="form-control" @bind-Value="@cliente.Cuando_Deben" />
            <ValidationMessage For="@(() => cliente.Cuando_Deben)" />
        </div>
    </div>




    <button class="btn btn-primary"  type="submit">aceptar</button>
    <button class="btn btn-secondary" @onclick="onCancel">Cancelar</button>

</EditForm>

@code {
    [Parameter] public Cliente cliente { get; set; }
    [Parameter] public EventCallback onValidSubmit { get; set; }
    [Parameter] public EventCallback onCancel { get; set; }
}
